const bcrypt = require("bcryptjs"); require("dotenv").config({ path: ".env.local" }); const { PrismaClient } = require("@prisma/client"); const { PrismaPg } = require("@prisma/adapter-pg"); const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL }); const prisma = new PrismaClient({ adapter }); console.log("Using email:", process.env.DHANAK_EMAIL); prisma.store.findUnique({ where: { email: process.env.DHANAK_EMAIL } }).then(async s => { if (!s) { console.log("Store not found"); return; } console.log("Store found:", s.name); const match = await bcrypt.compare(process.env.DHANAK_PASSWORD, s.password); console.log("Password match:", match); }).catch(e => console.log("Error:", e.message)).finally(() => process.exit());
