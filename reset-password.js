const bcrypt = require("bcryptjs"); require("dotenv").config({ path: ".env.local" }); const { PrismaClient } = require("@prisma/client"); const { PrismaPg } = require("@prisma/adapter-pg"); const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL }); const prisma = new PrismaClient({ adapter }); bcrypt.hash(process.env.DHANAK_PASSWORD, 10).then(hash => prisma.store.update({ where: { email: process.env.DHANAK_EMAIL }, data: { password: hash } })).then(() => console.log("Password updated successfully")).catch(e => console.log("Error:", e.message)).finally(() => process.exit());
