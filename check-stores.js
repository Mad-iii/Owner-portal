const { PrismaClient } = require("@prisma/client"); const { PrismaPg } = require("@prisma/adapter-pg"); require("dotenv").config({ path: ".env.local" }); const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL }); const prisma = new PrismaClient({ adapter }); prisma.store.findMany({ select: { id: true, email: true, slug: true, name: true } }).then(s => console.log(JSON.stringify(s, null, 2))).finally(() => process.exit());
